# Client-side environment variables
# Copy this file to .env.client and fill in the values

# Required on Sprite: tells the Wasp client to send API requests to the public URL
# instead of the default localhost:3001 (which is unreachable from the browser)
REACT_APP_API_URL=https://<your-sprite-name>.sprites.app
